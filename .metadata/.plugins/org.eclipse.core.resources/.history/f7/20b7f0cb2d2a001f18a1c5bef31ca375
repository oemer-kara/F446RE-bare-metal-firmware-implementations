
#include "uart.h"
#include "adc.h"
#include <stdio.h>

uint32_t g_sensor_value;

int main(void)
{

    uart2_txrx_init( );
    pa1_adc_init();


    while(1)
    {
        start_conversion();
    	sensor_value = adc_read();
    	printf("Sensor Value: %d \n\r", (int) g_sensor_value);
    }
}
